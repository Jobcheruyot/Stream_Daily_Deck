# ─────────────────────────────────────────────
# DailyDeck — requirements.txt
# Pinned to versions compatible with the app's
# actual imports: streamlit, pandas, numpy, plotly
# ─────────────────────────────────────────────

# Core UI framework
streamlit==1.42.0

# Data processing
pandas==2.2.3
numpy==1.26.4

# Visualisation (used extensively: go, px, imshow)
plotly==5.24.1

# ── Implicit / transitive deps kept for reproducibility ──

# Datetime helpers (used internally by pandas)
python-dateutil==2.9.0.post0
pytz==2025.2
